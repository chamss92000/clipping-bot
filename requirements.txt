# ---------------------------------------------------------------------------
# clipping-bot — dépendances
# Python 3.11+
# ---------------------------------------------------------------------------

# --- Bloc 1 : Détection des tendances ---
google-api-python-client==2.149.0     # YouTube Data API v3
requests==2.32.3                      # Twitch Helix REST + Kick scraping

# --- Bloc 2 : Téléchargement + Storage Google Drive ---
# yt-dlp : NE PAS épingler. YouTube casse régulièrement l'extraction des vieilles
# versions — il faut toujours la dernière. La CI fait `pip install -U yt-dlp`.
yt-dlp>=2026.8.19                     # YouTube / Twitch VOD / Kick download
google-auth==2.36.0                   # Auth (OAuth utilisateur + service account)
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1           # Flow OAuth local (authorize.py)
# (google-api-python-client fournit déjà le client Drive v3)

# --- Bloc 3 : Transcription ---
# faster-whisper (wheel CTranslate2) : installe sans compilation (openai-whisper
# casse sur setuptools récent : pkg_resources) et ~4x plus rapide sur CPU.
faster-whisper                        # ASR local CPU (modèle "base")

# --- Bloc 4 : Détection moments viraux (LLM) ---
google-generativeai==0.8.3            # Gemini 1.5 Flash

# --- Bloc 5 : Découpe + recadrage 9:16 ---
mediapipe==0.10.18                    # Face mesh / face detection
numpy==1.26.4                         # requis par mediapipe / traitement frames
opencv-python-headless==4.10.0.84     # lecture frames sans dépendance GUI (CI)
# FFmpeg est un binaire système (installé via apt/choco), pas un package pip.

# --- Bloc 6 : Sous-titres ---
# ASS généré à la main + FFmpeg. Pas de dépendance pip supplémentaire.

# --- Bloc 7 : Publication TikTok ---
# Upload-Post via API REST (requests, déjà présent).

# --- Génération contenu (relax + faceless) ---
Pillow>=10.0                          # dessin des frames (rendu 100% code, 0€)
edge-tts>=6.1                         # voix off IA gratuite (chaîne faceless)

# --- Commun ---
python-dotenv==1.0.1                  # chargement .env en local (no-op en CI)
tzdata==2024.2                        # base fuseaux IANA (zoneinfo sur Windows)
